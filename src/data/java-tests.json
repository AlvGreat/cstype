{
    "tests": [
        "for(int i = 0; i < 5; i++) {",
        "for(int i = 0; i < N; i++) {",
        "for(int i = 0; i < arr.length; i++) {",
        "for(int i = 0; i < str.length(); i++) {",
        "for(int j = i; j < count; j++) {",
        "for(type var : array) {",
        "for(int x : nums) {",
        "for(double a : arr) {",
        "int max = Integer.MAX_VALUE;",
        "int ans = 0; double d = 0.5; String str = \"hi\";",
        "ans = Math.max(ans, arr[i]);",
        "System.out.println(ans);",
        "System.out.print(ans);",
        "Scanner input = new Scanner(System.in);",
        "int userInput = input.nextInt();",
        "System.out.println(\"hello world!\")",
        "for(int i = 0; i < n; i++) arr[i] = input.next();",
        "int[] arr = new int[10];",
        "arr[2] = arr[0] + arr[1];",
        "int arr[] = {10, 20, 30};",
        "HashMap<String, Integer> map = new HashMap<>();",
        "Set<String> hash_Set = new HashSet<String>();",
        "Arrays.sort(axolotlList);",
        "if(str1.equals(str2)) return true;",
        "public void helper(char[] chars, int x) {",
        "public void printArray(int arr[], int length) {",
        "import java.util.Scanner;",
        "import java.util.*;",
        "if(x % 3 == 0 && x % 5 == 0) System.out.println(\"fizzbuzz\");",
        "if(x % 2 == 0) System.out.println(\"even\");"
    ]
}