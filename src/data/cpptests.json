{
    "tests": [
        "for(int i = 0; i < 5; i++) {",
        "for(int i = 0; i < N; i++) {",
        "for(int i = 0; i < nums.size(); i++) {",
        "for(int i = 0; i < v.size(); i++) {",
        "for(int j = i; j < count; j++) {",
        "for(auto& x : str) {",
        "for(int x : nums) {",
        "for(double a : arr) {",
        "for(string& s : str) {",
        "int max = INT_MAX, min = INT_MIN;",
        "int ans = 0; double d = 0.5; string str = \"hi\";",
        "ans = max(ans, v[i]);",
        "cout << ans << endl;",
        "cin >> x >> y;",
        "std::cout << \"hello world\"",
        "std::cin << myVariable;",
        "for(auto& a : v) cin >> a;",
        "while(N--) cout << v[N];",
        "vector<int> v(10, -1);",
        "v.push_back(i + 1);",
        "int arr[] = {10, 20, 30}",
        "unordered_map<int, int> m; m[i]++;",
        "set<int> s; s.insert(5);",
        "void helper(vector<int>& nums, int x) {",
        "void printArray(int arr[], int length) {",
        "int vectorSum(vector<int>& v) {",
        "priority_queue<int> pq; pq.pop();",
        "#include <stdio.h>;",
        "#include <string.h>;",
        "#include <math.h>;",
        "#include <bits/stdc++.h>;",
        "using namespace std;",
        "else if(x % 3 == 0 && x % 5 == 0) cout << \"fizzbuzz\";",
        "if(x % 2 == 0) cout << \"even\" << endl;"
    ]
}